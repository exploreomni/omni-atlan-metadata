# Core dependencies for metadata extraction
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Optional: Only needed for Temporal workflows
# temporalio>=1.5.0

# Optional: Only needed for publishing to Atlan (not required for output generation)
# atlan-sdk>=0.0.1

